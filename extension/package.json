{
  "name": "rook-extension",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "webpack --mode production",
    "test": "node --test \"test/**/*.test.mjs\"",
    "dev": "webpack --mode development --watch",
    "zip:chrome": "cd dist && zip -r ../rook-chrome.zip . && cd ..",
    "zip:firefox": "cd dist && zip -r ../rook-firefox.zip . && cd .."
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12",
    "linkedom": "^0.18.12",
    "webpack": "^5",
    "webpack-cli": "^5"
  }
}
